This folder should contain .zip files of compiled PPLs for the project (omitted in repo)
For builds, this expects two files:
- built_x32.zip
- built_x64.zip

Each should contain the PPLs built for this project